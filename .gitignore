# Rust
/target

# Local sherpa-onnx build cache (bypasses build-script download; not portable)
.sherpa-cache/

# Local config & secrets (DeepSeek key, bound-device UID, etc.)
/tuna.toml
/config.local.toml
.env

# Local data & caches (downloaded ECDICT, generated tuna.db, pre-synthesized audio).
# All artifacts — regenerated by `tuna build-deck`; force-add small seed files if ever needed.
/data/
/cache/

# macOS
.DS_Store
/data/etym_cache.jsonl

# Python
__pycache__/
*.pyc

# IDE working directories (local plans, session artifacts — not project content)
.trae/
.vscode/
.idea/
